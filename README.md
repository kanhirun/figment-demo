# Figment demo

Installing:

```
$ pnpm install
```

Running scripts:

```
$ solana-keygen new --outfile ~/.config/solana/id.json
$ solana airdrop <your-wallet-address>
$ pnpn run delegate-1-sol

$ pnpn run rewards-last-month

$ pnpn run undelegate <your-stake-account-address>
```
